# 기여 가이드라인

P-CODE 웹사이트 프로젝트에 기여해 주셔서 감사합니다. 이 문서는 프로젝트에 기여하는 방법에 대한 상세한 가이드라인을 제공합니다.

## 목차

- [행동 강령](#행동-강령)
- [기여 절차](#기여-절차)
- [개발 환경 설정](#개발-환경-설정)
- [코드 스타일 가이드](#코드-스타일-가이드)
- [브랜치 관리 전략](#브랜치-관리-전략)
- [커밋 메시지 규칙](#커밋-메시지-규칙)
- [테스트 가이드](#테스트-가이드)
- [문서화 가이드](#문서화-가이드)
- [리뷰 프로세스](#리뷰-프로세스)
- [보안 취약점 보고](#보안-취약점-보고)

## 행동 강령

우리 프로젝트는 개방적이고 존중하는 커뮤니티를 지향합니다. 모든 참여자는 다음 원칙을 준수해야 합니다:

- 다른 참여자를 존중하고 포용적인 언어를 사용합니다.
- 건설적인 비판을 장려하되, 개인적인 공격은 삼갑니다.
- 커뮤니티의 경험을 모두에게 긍정적으로 만드는 데 집중합니다.
- 기술적 토론에서도 공손하고 전문적인 태도를 유지합니다.

## 기여 절차

### 1. 이슈 확인 및 생성

프로젝트에 기여하기 전에 GitHub 이슈를 확인하세요. 작업하려는 내용이 이미 다른 사람에 의해 진행 중인지 확인합니다.

- 버그를 발견했다면 **버그 리포트** 템플릿을 사용하여 이슈를 생성하세요.
- 새로운 기능을 제안하려면 **기능 요청** 템플릿을 사용하세요.
- 이슈 제목은 명확하게 작성하고, 내용은 가능한 한 자세히 설명하세요.

### 2. 저장소 포크 및 클론

1. GitHub에서 저장소를 포크합니다.
2. 포크한 저장소를 로컬에 클론합니다:
   ```bash
   git clone https://github.com/YOUR_USERNAME/pcode_web.git
   cd pcode_web
   ```
3. 원본 저장소를 업스트림으로 추가합니다:
   ```bash
   git remote add upstream https://github.com/original-owner/pcode_web.git
   ```

### 3. 브랜치 생성

작업을 시작하기 전에 최신 코드를 가져오고 새 브랜치를 생성하세요:
```bash
git checkout main
git pull upstream main
git checkout -b feature/기능명
# 또는
git checkout -b fix/버그명
```

### 4. 코드 작성 및 테스트

- 코드 스타일 가이드라인을 준수하세요.
- 변경 사항에 대한 테스트를 추가하세요.
- 모든 테스트가 통과하는지 확인하세요.
- 변경 사항이 기존 기능을 손상시키지 않는지 확인하세요.

### 5. 커밋 및 푸시

변경 사항을 커밋하고 푸시하세요:
```bash
git add .
git commit -m "유형: 간결하고 명확한 커밋 메시지"
git push origin feature/기능명
```

자세한 커밋 메시지 형식은 [커밋 메시지 규칙](#커밋-메시지-규칙)을 참조하세요.

### 6. Pull Request 생성

GitHub에서 원본 저장소에 Pull Request(PR)를 생성하세요. PR 설명에는 다음 내용을 포함하세요:

- 변경 사항의 목적과 배경
- 관련 이슈 번호 (예: "Fixes #123")
- 테스트 방법 및 결과
- 스크린샷 (UI 변경이 있는 경우)

### 7. 코드 리뷰 및 수정

- PR이 생성되면 다른 팀원들의 리뷰를 기다립니다.
- 리뷰어의 피드백에 따라 코드를 수정합니다.
- 수정 후에는 PR에 추가 커밋을 푸시하세요.

### 8. 병합

- 최소 1명 이상의 승인을 받아야 병합이 가능합니다.
- CI 테스트가 모두 통과해야 병합이 가능합니다.
- 모든 검토 의견이 해결되면 PR이 병합됩니다.

## 개발 환경 설정

### 필요 조건

- Python 3.9 이상
- Node.js 14 이상
- Docker 및 Docker Compose
- Git

### 로컬 환경 설정

1. 의존성 설치:
   ```bash
   # 백엔드 의존성
   cd backend
   pip install -r requirements.txt
   
   # 프론트엔드 의존성 (향후 추가 예정)
   # cd frontend
   # npm install
   ```

2. 환경 변수 설정:
   - 루트 디렉토리에 `.env` 파일을 생성하고 필요한 환경 변수를 설정합니다.
   - 개발 환경에서만 사용할 환경 변수는 `.env.development`에 추가합니다.
   - 절대로 민감한 정보를 Git에 커밋하지 마세요.

3. Docker 환경 시작:
   ```bash
   docker-compose up -d
   ```

## 코드 스타일 가이드

### Python 코드

- [PEP 8](https://www.python.org/dev/peps/pep-0008/) 스타일 가이드를 따르세요.
- 들여쓰기는 4칸 공백을 사용하세요.
- 최대 줄 길이는 100자를 유지하세요.
- 함수, 클래스, 메소드에 적절한 문서 문자열(docstring)을 추가하세요.
- 가독성 있는 변수명과 함수명을 사용하세요.

### HTML/CSS 코드

- HTML은 시맨틱 태그를 사용하세요.
- CSS는 BEM 방법론을 따르세요.
- 들여쓰기는 4칸 공백을 사용하세요.
- 모바일 우선 접근법으로 반응형 디자인을 구현하세요.

### JavaScript 코드

- [Airbnb JavaScript 스타일 가이드](https://github.com/airbnb/javascript)를 따르세요.
- 들여쓰기는 2칸 공백을 사용하세요.
- ES6+ 문법을 사용하세요.
- 세미콜론을 사용하세요.

## 브랜치 관리 전략

우리 프로젝트는 GitHub Flow를 기반으로 한 브랜치 관리 전략을 사용합니다:

- **main**: 안정 버전 브랜치, 항상 배포 가능한 상태를 유지합니다.
- **feature/***: 새로운 기능 개발을 위한 브랜치
- **fix/***: 버그 수정을 위한 브랜치
- **docs/***: 문서 관련 작업을 위한 브랜치
- **refactor/***: 코드 리팩토링을 위한 브랜치
- **test/***: 테스트 코드 작성을 위한 브랜치

## 커밋 메시지 규칙

커밋 메시지는 [Conventional Commits](https://www.conventionalcommits.org/ko/v1.0.0/) 형식을 따릅니다:

```
<유형>[적용 범위(선택사항)]: <설명>

[본문(선택사항)]

[꼬리말(선택사항)]
```

### 유형

- **기능**: 새로운 기능 추가
- **수정**: 버그 수정
- **문서**: 문서 변경 사항
- **스타일**: 코드 스타일 변경 (코드 동작에 영향을 주지 않는 변경)
- **리팩터**: 코드 리팩토링
- **테스트**: 테스트 코드 추가 또는 수정
- **빌드**: 빌드 시스템 또는 외부 의존성에 영향을 주는 변경
- **ci**: CI 구성 파일 및 스크립트 변경
- **설정**: 기타 설정 변경

### 예시

```
기능(인증): 소셜 로그인 기능 추가

- Google OAuth2 로그인 추가
- 프로필 정보 동기화 로직 구현

관련 이슈: #123
```

## 테스트 가이드

모든 코드 변경에는 적절한 테스트가 포함되어야 합니다:

### 백엔드 테스트

- 단위 테스트는 `pytest`를 사용합니다.
- API 테스트는 `pytest`와 함께 `Flask` 테스트 클라이언트를 사용합니다.
- 테스트 파일은 `tests/` 디렉토리에 위치해야 합니다.
- 테스트 파일명은 `test_*.py` 형식이어야 합니다.

### 프론트엔드 테스트 (향후 추가 예정)

- 단위 테스트는 Jest를 사용합니다.
- UI 컴포넌트 테스트는 Testing Library를 사용합니다.
- E2E 테스트는 Cypress를 사용합니다.

### 테스트 실행

```bash
# 백엔드 테스트
cd backend
python -m pytest

# 특정 테스트 실행
python -m pytest tests/test_auth.py
```

## 문서화 가이드

- 코드 내 주석은 코드의 '왜'에 집중하세요. (어떻게가 아닌)
- API 엔드포인트는 문서화되어야 합니다. (OpenAPI/Swagger 사용 예정)
- 중요한 기능이나 컴포넌트는 별도의 문서 파일로 작성하세요.
- `docs/` 디렉토리에 문서를 추가하세요.

## 리뷰 프로세스

코드 리뷰는 프로젝트의 품질을 유지하는 데 중요합니다:

- 리뷰어는 코드 스타일, 로직, 성능, 보안 등을 검토합니다.
- 건설적인 피드백을 제공하고 받으세요.
- 코드 리뷰 의견에 대해 적절히 대응하세요.
- 논쟁이 발생하면 팀 리더에게 중재를 요청하세요.

## 보안 취약점 보고

보안 취약점은 공개 이슈로 보고하지 마세요:

1. 프로젝트 관리자에게 직접 이메일을 보내세요.
2. 취약점에 대한 상세한 설명과 재현 방법을 제공하세요.
3. 가능하다면 해결책을 제안하세요.

보안 취약점은 최우선 순위로 처리됩니다. 